You are an Azure VM CPU and RAM pattern analysis expert. Analyze each VM based on the LAST 15 DAYS of daily metrics. Return ONLY valid JSON. NO explanations. NO markdown.

VM Data (15 days):
{vms_json}

CRITICAL RULES:
- Base analysis ONLY on the provided dailyData array
- Use EXACT values from the data - no assumptions or examples
- All fields must have actual values (no nulls when data exists)
- If data is insufficient, mark hasPattern=false with explanation

For each VM's dailyData (maxCpuPercent, maxUsedRamPercent with timestamps):

1. CPU PATTERN ANALYSIS:
   - hasPattern = true if same weekday AND same hour (±2 hours) shows peaks for ≥3 occurrences
   - hasPattern = false if less than 3 peaks or random timing
   - description = Specific pattern (e.g., "Peaks every Monday 14:00-16:00, avg 75% CPU")
   - peakDay = Day with highest avg CPU (MONDAY/TUESDAY/WEDNESDAY/THURSDAY/FRIDAY/SATURDAY/SUNDAY)
   - peakHour = Hour (0-23) with highest avg CPU across all days
   - severity = CRITICAL (CPU > 85% on ≥3 days OR single spike >95%)
   - severity = HIGH (CPU > 75% on ≥3 days)
   - severity = MEDIUM (CPU > 60% on ≥3 days)
   - severity = LOW (all other cases)

2. RAM PATTERN ANALYSIS:
   - hasPattern = true if similar usage pattern repeats on same days/times (min 3 occurrences)
   - description = Specific pattern (e.g., "RAM gradually increases from 40% to 75% over 15 days")
   - severity = CRITICAL (RAM > 90% on ≥3 days)
   - severity = HIGH (RAM > 85% on ≥2 days)
   - severity = MEDIUM (RAM > 80% on any day)
   - severity = LOW (all RAM values ≤79%)

3. ANOMALY DETECTION (check ALL conditions):
   - detected = true if ANY of:
     a) Peak > 2x the 15-day average CPU
     b) Off-hour peak (outside 8AM-8PM) exceeds normal daytime average by 50%
     c) RAM increases by >15% over 5 consecutive days (potential memory leak)
     d) Spike on weekend when weekday pattern is low
   - details = Specific: "On 2026-05-18 at 03:00, CPU spike 94% (avg 32%)"

4. RECOMMENDATION:
   - action = SCALE_UP (RAM >85% OR CPU >80% consistently for 5+ days)
   - action = SCALE_DOWN (RAM <30% AND CPU <20% for entire 15-day period)
   - action = INVESTIGATE (anomalies detected OR patterns unpredictable)
   - action = NONE (stable usage within normal ranges)
   - suggestion = Specific actionable advice (e.g., "Increase VM from Standard_B2s to B2ms")

Return EXACT JSON structure:
{
  "analyses": [
    {
      "vmId": 0,
      "vmName": "",
      "cpuPattern": {
        "hasPattern": false,
        "description": "No consistent pattern detected",
        "peakDay": null,
        "peakHour": null,
        "severity": "LOW"
      },
      "ramPattern": {
        "hasPattern": false,
        "description": "No consistent pattern detected",
        "severity": "LOW"
      },
      "anomaly": {
        "detected": false,
        "details": null
      },
      "recommendation": {
        "action": "NONE",
        "suggestion": "No action required at this time"
      }
    }
  ]
}